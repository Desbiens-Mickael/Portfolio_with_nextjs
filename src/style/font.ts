import { Inter, Paytone_One } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });
const paytone = Paytone_One({ subsets: ["latin"], weight: "400" });

export { inter, paytone };
